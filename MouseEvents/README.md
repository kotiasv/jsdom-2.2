# Практическая работа по теме MouseEvents

Контрольная точка 1 по разделу Mouse Events

## Структура проекта

Сама работа написана на TS - `./src`.

Для конвертации в JS используется `tsc` и `tsc-alias`. - `./build`.